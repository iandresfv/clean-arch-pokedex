apiVersion: v1
kind: Service
metadata:
  name: pokedex-api
  labels:
    app.kubernetes.io/name: pokedex-api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: pokedex-api
  ports:
    - name: http
      port: 80
      targetPort: http
